name: My project deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify workspace
        run: |
          pwd
          ls -la

      - name: Dummy deploy step
        run: echo "Workflow is working correctly"
